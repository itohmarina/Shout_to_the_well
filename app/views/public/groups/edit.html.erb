<div>
  <%= flash[:notice] %>
</div>

<div class='container'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <%= render 'public/users/info', user: @user %>
    </div>
    <div class='col-md-8 offset-md-1'>
  		<h2>Group infomation</h2>
  		<%= form_with model: @group, url: public_group_path, method: :patch do |f| %>
  		  <%= f.text_area :name, placeholder:"グループ名を入れてください", cols:"50", rows:"1" %><br>
  		  <%= f.text_area :introduction, placeholder:"グループ紹介を入れてください", cols:"50", rows:"2" %><br>

  		  <%= f.submit "グループ情報を更新する",class:"btn btn-primary" %>
      <% end %>
    </div>
    <div>
      使用方法
      グループページの「招待する」ボタンから、ほかのユーザーをグループに招待することができます。
      相手が招待を受諾すると、グループ内で会話することができるようになります。
    </div>
  </div>
</div>
