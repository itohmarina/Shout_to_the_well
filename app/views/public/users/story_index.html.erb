<h2><%= "#{@user.name}さんのストーリー"</h2>

<table class="table table-hover table-inverse">
    <tbody>
      <% if @stories.present? %>
        <% @stories.each do |story| %>
          <tr>
            <td><%= story.title %></td>
            <td><%= story.body %></td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
</table>