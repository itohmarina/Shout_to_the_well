<h2>投書箱</h2>
<div>
  内容を確認してください。
  投書の内容は以下でよろしいでしょうか？
</div>
<div>
  <%= form_with model: @suggestion, url: public_suggestions_path, method: :post do |f| %>
    <%= @suggestion.title %>
    <%= f.hidden_field :title, value: @suggestion.title %><br>
    <%= @suggestion.body %>
    <%= f.hidden_field :body, value: @suggestion.body %><br>
    <%= f.submit "修正する", name: :back, class: "btn btn-info" %>
    <%= f.submit "投書する", class: "btn btn-primary" %>
  <% end %>
</div>
