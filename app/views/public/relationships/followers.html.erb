<h2>フォロワー一覧</h2>

<% if @users.present? %>
  <% @users.each do |user| %>
    <div>
      <%= image_tag user.get_user_image %>
      <%= link_to "#{user.name}", public_user_path(user.id) %>
    </div>
  <% end %>
<% end %>
